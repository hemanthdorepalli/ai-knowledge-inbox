# Gemini powers both embeddings and chat. Free key at https://aistudio.google.com/apikey
GEMINI_API_KEY=your-gemini-api-key
EMBEDDING_MODEL=gemini-embedding-001
EMBEDDING_DIM=768

# Chat runs on Gemini via its OpenAI-compatibility endpoint (uses GEMINI_API_KEY
# above). To switch providers -- e.g. to Groq -- set CHAT_BASE_URL, CHAT_MODEL,
# and CHAT_API_KEY; no code changes needed.
CHAT_BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai/
CHAT_MODEL=gemini-2.5-flash
# CHAT_API_KEY=   # optional; defaults to GEMINI_API_KEY when unset

# Supabase Postgres connection string.
# Dashboard -> Project Settings -> Database -> Connection string -> URI
# (replace [YOUR-PASSWORD] with your database password).
SUPABASE_DB_URL=postgresql://postgres.xxxx:[YOUR-PASSWORD]@aws-0-region.pooler.supabase.com:5432/postgres

# Supabase API — used to verify the user's login token on each request.
# Dashboard -> Project Settings -> API
SUPABASE_URL=https://xxxx.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key

CORS_ORIGINS=http://localhost:5173

# Token budget per user (covers ingest + chat), refilled on a rolling window.
# The window resets lazily: the first request after it expires zeroes the
# counter and restarts the clock, so there is nothing to schedule.
DEFAULT_TOKEN_QUOTA=1000000
QUOTA_WINDOW_HOURS=24

# MCP tool calling: max tool-calling round trips per question.
MCP_MAX_TOOL_ROUNDS=4
# Public hostname this backend is served on (no scheme), e.g.
# "my-api.onrender.com". Usually NOT needed -- it's auto-detected on Render via
# RENDER_EXTERNAL_HOSTNAME. Set it only to override, or when deploying somewhere
# we can't auto-detect. (The demo MCP server keeps DNS-rebinding protection on
# and validates the Host header against this.)
PUBLIC_HOST=
# Dev-only: allows connecting an MCP server on localhost/private IPs for local
# testing. Must stay false (the default) in any real deployment.
ALLOW_LOCAL_MCP_URLS=false
